\documentclass[a4paper,draft]{article}%a4纸张，打印超过右边界，会显示黑条标志。
\usepackage{ctexcap,titlesec,amsfonts,amssymb}%ctexcap宏包用于ctex宏包引用后的标题命令格式的修改，titlesec是英文的标题设置宏包。
\usepackage{ntheorem}%这是修改\newtheoren的格式的宏包,\newtheoren 是证明环境定义命令
\pagestyle{headings}%headings格式，对于具有oneside项的article，它的页眉为节标题。对于具有oneside项的book或report，页眉为章标题
\begin{document}
$$\frac{1}{2}+\sum^n_{i=1}\cos{ih}=\frac{\sin{(n+\frac{1}{2})}h}{2\sin{\frac{1}{2}h}}$$
$\sum_{n=1}^{\infty}{a_i}{b_i}$为实数级数
阿贝尔判别法:
$\sum_{n=1}^{\infty}b_n$收敛，$a_n$单调有界，则$\sum_{n=1}^{\infty}{a_i}{b_i}$收敛
狄利克雷判别法:
$如果B_i有界,a_n单调趋于0，则\sum_{i=1}^m{a_i}{b_i}收敛$
阿贝尔引理:
$(如果a_i都不递增或都不递减，而和B_i都以数L为上界：|B_i|\leq L)\rightarrow|\sum_{i=1}^m{a_i}{b_i}|\leq L(|a_1|+2|a_m|)$
阿贝尔变换:
$\sum_{i=1}^m{a_i}{b_i}=a_mB_m-\sum_{i=1}^{m-1}{(a_{i+1}-a_i)B_i}$
柯西施瓦茨不等式
$\left( \sum_{k=1}^n a_k b_k \right)^{2}\leq \left( \sum_{k=1}^n a_k^2 \right) \left( \sum_{k=1}^n b_k^2 \right)$
fourier series:
于$[0,pi]$上展开到$[-pi,pi]$上的迅速收敛级数$r(x)$为$2k-1$次多项式：$r(0)=f(0),r'(0)=f'(0),\dots,r^{(k-1)}(0)=f^{(k-1)}(0)$
$r(-\pi)=f(\pi),r'(-\pi)=f'(\pi),\dots,r^{(k-1)}(-\pi)=f^{(k-1)}(\pi)$
$\Sum_{n=1}^{\infty}\frac{sin{nz}}{n}=\frac{\pi-z}{2} (0<z<2\pi)$
$g(x)=$\Sum_{n=1}^{\infty}\frac{A_n}{n}\cos{nx}$
\end{document}
